const path = require('path');

module.exports = {
  'models-path': path.resolve('src', 'modules', 'DB', 'postgres', 'models'),
  'migrations-path': path.resolve('src', 'modules', 'DB', 'postgres', 'migrations'),
};